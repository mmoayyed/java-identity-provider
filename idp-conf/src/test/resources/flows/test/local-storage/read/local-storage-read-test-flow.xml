<flow xmlns="http://www.springframework.org/schema/webflow"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.springframework.org/schema/webflow http://www.springframework.org/schema/webflow/spring-webflow.xsd">

    <!-- For this test flow to read from local storage, get variables from the request instead of the StorageService. -->
    <on-start>
        <evaluate expression="requestParameters.localStorageKey" result="flowScope.localStorageKey" />
        <evaluate expression="requestParameters.localStorageVersion" result="flowScope.localStorageVersion" />
    </on-start>

    <action-state id="Initialize">
        <evaluate expression="InitializeProfileRequestContext" />
        <evaluate expression="'proceed'" />
        <transition on="proceed" to="LocalStorageRead" />
    </action-state>

    <subflow-state id="LocalStorageRead" subflow="local-storage/read">
        <input name="calledAsSubflow" value="true" />

         <!-- The input attributes contain the strings actually written to local storage. -->
        <input name="localStorageKey" type="string" value="flowScope.localStorageKey" />
        <input name="localStorageVersion" type="string" value="flowScope.localStorageVersion" />

        <!-- Store the returned parameters in flow scope for testing purposes. -->
        <transition to="LocalStorageView">
            <set name="flowScope.localStorageException" value="currentEvent.attributes.localStorageException" />
            <set name="flowScope.localStorageSuccess" value="currentEvent.attributes.localStorageSuccess" />
            <set name="flowScope.localStorageSupported" value="currentEvent.attributes.localStorageSupported" />
            <set name="flowScope.localStorageValue" value="currentEvent.attributes.localStorageValue" />
            <set name="flowScope.localStorageVersion" value="currentEvent.attributes.localStorageVersion" />
        </transition>
    </subflow-state>

    <view-state id="LocalStorageView" view="/local-storage-test/local-storage-display">
        <!-- TODO -->
    </view-state>

    <end-state id="proceed" />

    <bean-import resource="../local-storage-test-beans.xml" />

</flow>
