<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:c="http://www.springframework.org/schema/c"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd"
                           
       default-init-method="initialize"
       default-destroy-method="destroy">

    <!--
    This file configures the IdP with the set of SAML bindings to support for sending messages back through
    a browser to SPs. Separate binding lists are provided for SAML 1, SAML 2 SSO, and SAML 2 SLO.
    
    For advanced use cases, it's possible to configure an activationCondition property that can evaluate the
    profile request context to determine whether a binding applies, for example based on the SP identity.
    
    In most cases, simply leaving this alone, or reordering the bindings to fit your preferences, is enough. 
    -->
    
    <bean id="shibboleth.BindingDescriptor" class="org.opensaml.saml.common.binding.BindingDescriptor" abstract="true" />
    
    <util:list id="shibboleth.OutgoingSAML1Bindings">
        <bean parent="shibboleth.BindingDescriptor" c:id="urn:oasis:names:tc:SAML:1.0:profiles:browser-post" />
        <bean parent="shibboleth.BindingDescriptor" c:id="urn:oasis:names:tc:SAML:1.0:profiles:artifact-01" />
    </util:list>

    <util:list id="shibboleth.OutgoingSAML2SSOBindings">
        <bean parent="shibboleth.BindingDescriptor" c:id="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST" />
        <bean parent="shibboleth.BindingDescriptor" c:id="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST-SimpleSign" />
        <bean parent="shibboleth.BindingDescriptor" c:id="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST-Artifact" />
    </util:list>

    <util:list id="shibboleth.OutgoingSAML2SLOBindings">
        <bean parent="shibboleth.BindingDescriptor" c:id="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Redirect" />
        <bean parent="shibboleth.BindingDescriptor" c:id="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST" />
        <bean parent="shibboleth.BindingDescriptor" c:id="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST-SimpleSign" />
        <bean parent="shibboleth.BindingDescriptor" c:id="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST-Artifact" />
    </util:list>

</beans>
