<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:context="http://www.springframework.org/schema/context"
    xmlns:util="http://www.springframework.org/schema/util" xmlns:p="http://www.springframework.org/schema/p" xmlns:c="http://www.springframework.org/schema/c"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd"

    default-init-method="initialize" default-destroy-method="destroy">

    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer"
        p:placeholderPrefix="%{" p:placeholderSuffix="}" />

    <bean id="shibboleth.GenerateSigningKey" class="net.shibboleth.utilities.java.support.security.SelfSignedCertificateGenerator"
        init-method="generate"
        p:hostName="%{idp.entityID}"
        p:privateKeyFile="%{idp.signing.key}"
        p:certificateFile="%{idp.signing.cert}"
        p:URISubjectAltNames-ref="shibboleth.URISubjectAltNames" />
    
    <bean id="shibboleth.GenerateEncryptionKey" class="net.shibboleth.utilities.java.support.security.SelfSignedCertificateGenerator"
        init-method="generate"
        p:hostName="%{idp.entityID}"
        p:privateKeyFile="%{idp.encryption.key}"
        p:certificateFile="%{idp.encryption.cert}"
        p:URISubjectAltNames-ref="shibboleth.URISubjectAltNames" />
    
    <bean id="shibboleth.GenerateTLSKey" class="net.shibboleth.utilities.java.support.security.SelfSignedCertificateGenerator"
        init-method="generate"
        p:hostName="%{idp.entityID}"
        p:keystoreFile="%{idp.home}/creds/idp-tls.p12"
        p:keystorePassword="changeit"
        p:URISubjectAltNames-ref="shibboleth.URISubjectAltNames" />

    <util:list id="shibboleth.URISubjectAltNames">
        <value>%{idp.entityID}/idp/shibboleth</value>
    </util:list>


</beans>
